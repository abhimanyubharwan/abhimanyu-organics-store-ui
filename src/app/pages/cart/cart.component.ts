import { Component, inject } from '@angular/core'; import { CurrencyPipe } from '@angular/common'; import { CatalogService } from '../../services/catalog.service'; @Component({selector:'app-cart',standalone:true,imports:[CurrencyPipe],templateUrl:'./cart.component.html'}) export class CartComponent{catalog=inject(CatalogService);get total(){return this.catalog.cart().reduce((a,b)=>a+b.price,0)}}
