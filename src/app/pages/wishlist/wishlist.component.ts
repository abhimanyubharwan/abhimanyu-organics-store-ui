import { Component, inject } from '@angular/core'; import { CatalogService } from '../../services/catalog.service'; import { ProductCardComponent } from '../../components/product-card/product-card.component'; @Component({selector:'app-wishlist',standalone:true,imports:[ProductCardComponent],templateUrl:'./wishlist.component.html'}) export class WishlistComponent{catalog=inject(CatalogService);get list(){return this.catalog.products.filter(p=>this.catalog.wishlist().includes(p.id))}}
