import { Component, inject } from '@angular/core'; import { ActivatedRoute, RouterLink } from '@angular/router'; import { CatalogService } from '../../services/catalog.service';
@Component({selector:'app-product',standalone:true,imports:[RouterLink],templateUrl:'./product.component.html'}) export class ProductComponent{catalog=inject(CatalogService);route=inject(ActivatedRoute);get p(){return this.catalog.products.find(x=>x.id===this.route.snapshot.paramMap.get('id'))||this.catalog.products[0]}}
