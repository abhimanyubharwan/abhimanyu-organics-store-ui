import { Component, inject } from '@angular/core'; import { CommonModule } from '@angular/common'; import { CatalogService } from '../../services/catalog.service'; import { ProductCardComponent } from '../../components/product-card/product-card.component'; @Component({selector:'app-seasonal',standalone:true,imports:[CommonModule,ProductCardComponent],templateUrl:'./seasonal.component.html'}) export class SeasonalComponent{catalog=inject(CatalogService);get list(){return this.catalog.products.filter(p=>p.category==='Seasonal Fruits')}}
